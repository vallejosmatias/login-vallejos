<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <link rel="stylesheet" href={{style}}>
</head>
<body>
     <div class="container">
        {{#if session.user}}
            <p>Bienvenido, {{session.user}} (Rol: {{session.role}})</p>
            <form action="/logout" method="POST">
                <button type="submit">Logout</button>
            </form>
        {{/if}}
        
        {{{body}}}
    </div>
</body>
</html>